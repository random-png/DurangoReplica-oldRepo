<Page
    x:Class="XboxMusic.Pages.RemakeExclusive.UnfaithfulSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XboxMusic.Pages.RemakeExclusive"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d" RequestedTheme="Dark">
    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,2.5">
            <GradientStop Color="#000000" Offset="0"/>
            <GradientStop Color="{ThemeResource SystemAccentColor}" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromHorizontalOffset="500" FromVerticalOffset="0" IsStaggeringEnabled="True"/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <MediaElement AutoPlay="True" Source="ms-appx:///Assets/Sounds/PanelUnfold.wav" Visibility="Collapsed" IsHitTestVisible="False"/>
        <TextBlock x:Name="ScreenName" Text="Unfaithful mode options" VerticalAlignment="Top" FontSize="46" Margin="100,156,0,0" OpticalMarginAlignment="TrimSideBearings" FontWeight="Light" CharacterSpacing="40" FontFamily="Segoe UI"/>
        <ScrollViewer Grid.Row="1" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Hidden" VerticalScrollMode="Disabled" VerticalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Horizontal" Padding="100,0,92,0" Spacing="80">
                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="500" FromVerticalOffset="0" IsStaggeringEnabled="True"/>
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>
                <Grid Margin="0,17,0,132" RowSpacing="18" MaxWidth="462">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="72"/>
                    </Grid.RowDefinitions>
                    <TextBlock FontFamily="Segoe UI" FontSize="36" FontWeight="SemiLight" OpticalMarginAlignment="TrimSideBearings" Text="How do I get back here?" TextLineBounds="Tight"/>
                    <ScrollViewer x:Name="ReturnInfoScrollViewer" Grid.Row="1" IsFocusEngagementEnabled="True" VerticalScrollBarVisibility="Hidden" BorderBrush="{ThemeResource ItemBorder}" BorderThickness="1" FocusDisengaged="ReturnInfoScrollViewer_FocusDisengaged">
                        <TextBlock FontSize="24" OpticalMarginAlignment="TrimSideBearings" FontFamily="Segoe UI" TextWrapping="WrapWholeWords" Padding="8,6">
                            <Run Foreground="{ThemeResource SystemAccentColor}">TODO: Add instructions later, and make these functional. Some defaults will be set.</Run>
                            <LineBreak/>
                            <LineBreak/><Run Foreground="{ThemeResource ApplicationSecondaryForegroundThemeBrush}">Unfaithful mode's visibility is reset every session, but any settings you change are saved.</Run>
                            <LineBreak/>
                            <LineBreak/>The Xbox System Software contains a heavily modified Hyper-V hypervisor (known as NanoVisor) as its host OS and two partitions. One of the partitions, the "Exclusive" partition is a custom virtual machine (VM) for games; the other partition, the "Shared" partition is a custom VM for running multiple apps including the OS. The system software's interface uses a geometrical placement of squares and rectangular items that scrolls as a continuous vertical line, using the Metro design language that is also seen in Windows 8, Windows 10, and other Microsoft products. When Microsoft upgraded the Windows 8-based Core to a Windows 10-based one, they made a tour of the new user interface up on Xbox Wire, promising faster, easier navigation, improved community features and the return of Xbox Avatars. The UI includes a HOME screen, consisting of the top bar, the screenshot viewer ("Album") Icons, and shortcuts to the Microsoft Store, News, and Settings. The main feature on the home screen is a list of the most recently played games. Selecting any given title will give users more information about announcements, achievements, social activity and so forth. It is also more focused on the actual games they are playing, which was part and parcel of the company's new direction under Phil Spencer, then-head of Microsoft's Xbox division.
                        </TextBlock>
                    </ScrollViewer>
                    <Button x:Name="UnfaithfulSettingResetButton" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="20" FontWeight="SemiBold" BorderThickness="1" BorderBrush="#28FFFFFF" ClickMode="Press">
                        <TextBlock CharacterSpacing="24">RESET ALL SETTINGS</TextBlock>
                    </Button>
                </Grid>
                <RelativePanel x:Name="StylesSettingsBlock" RelativePanel.Below="ScreenName" Margin="0,17,0,0">
                    <TextBlock x:Name="SectionHeader1" Text="APP STYLES" Style="{ThemeResource GroupHeaderTextBlockStyle}"/>
                    <StackPanel x:Name="AppStyleSettingsList" RelativePanel.Below="SectionHeader1">
                        <ComboBox x:Name="AccentDropdown" Header="Accent color" IsEnabled="False" Description="Linked to branding" SelectionChanged="AccentDropdown_SelectionChanged">
                            <ComboBoxItem x:Name="AcccentFallback" Tag="XboxGreen">Xbox Green</ComboBoxItem>
                            <ComboBoxItem Tag="GrooveBlue">Groove Blue</ComboBoxItem>
                            <ComboBoxItem Tag="SystemColor">System accent color</ComboBoxItem>
                            <ComboBoxItem Tag="CustomColor">Custom</ComboBoxItem>
                        </ComboBox>
                        <ComboBox x:Name="BrandingDropdown" Header="Branding" Description="Only applies internally" SelectionChanged="BrandingDropdown_SelectionChanged">
                            <ComboBoxItem Tag="XboxMusic">Xbox Music</ComboBoxItem>
                            <ComboBoxItem Tag="GrooveMusic">Groove Music</ComboBoxItem>
                        </ComboBox>
                        <CheckBox x:Name="AccentBrandingUnlinkCheckbox" Content="Unlink accent color and branding" Click="AccentBrandingUnlinkCheckbox_Toggled"/>
                    </StackPanel>
                </RelativePanel>
                <RelativePanel x:Name="TempBlock" Margin="0,17,0,0">
                    <TextBlock x:Name="SectionHeader2" Text="name later" Style="{ThemeResource GroupHeaderTextBlockStyle}"/>
                </RelativePanel>
                <RelativePanel x:Name="AboutBlock" Margin="0,17,0,0">
                    <TextBlock x:Name="AboutSectionHeader" Text="ABOUT THIS APP" Style="{ThemeResource GroupHeaderTextBlockStyle}"/>
                    <StackPanel RelativePanel.Below="AboutSectionHeader">
                        <Button Content="Xbox Music app replica" Width="424" Height="100" Background="#00222222" FontSize="24" HorizontalContentAlignment="Left" Padding="18,0"/>
                        <ProgressRing IsActive="True" Style="{ThemeResource ContentXboxProgressRing}"/>
                    </StackPanel>
                </RelativePanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
