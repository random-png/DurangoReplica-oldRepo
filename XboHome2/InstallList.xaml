<Page
    x:Class="XboxHome2.InstallList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XboxHome2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style TargetType="GridViewItem">
            <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
            <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
            <Setter Property="Background" Value="{ThemeResource GridViewItemBackground}"/>
            <Setter Property="Foreground" Value="{ThemeResource GridViewItemForeground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="TabNavigation" Value="Local"/>
            <Setter Property="IsHoldingEnabled" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="MinWidth" Value="{ThemeResource GridViewItemMinWidth}"/>
            <Setter Property="MinHeight" Value="{ThemeResource GridViewItemMinHeight}"/>
            <Setter Property="AllowDrop" Value="False"/>
            <Setter Property="FocusVisualPrimaryThickness" Value="4"/>
            <Setter Property="FocusVisualSecondaryThickness" Value="4"/>
            <Setter Property="FocusVisualPrimaryBrush" Value="White"/>
            <Setter Property="FocusVisualSecondaryBrush" Value="Black"/>
            <Setter Property="UseSystemFocusVisuals" Value="{StaticResource UseSystemFocusVisuals}"/>
            <Setter Property="FocusVisualMargin" Value="-8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewItem">
                        <ListViewItemPresenter x:Name="Root" CheckMode="{ThemeResource GridViewItemCheckMode}" CheckBoxBrush="{ThemeResource GridViewItemCheckBoxBrush}" CheckBrush="{ThemeResource GridViewItemCheckBrush}" ContentMargin="{TemplateBinding Padding}" ContentTransitions="{TemplateBinding ContentTransitions}" DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}" DragBackground="{ThemeResource GridViewItemDragBackground}" DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}" DragForeground="{ThemeResource GridViewItemDragForeground}" FocusVisualMargin="{TemplateBinding FocusVisualMargin}" FocusBorderBrush="{ThemeResource GridViewItemFocusBorderBrush}" FocusSecondaryBorderBrush="{ThemeResource GridViewItemFocusSecondaryBorderBrush}" FocusVisualPrimaryThickness="{TemplateBinding FocusVisualPrimaryThickness}" FocusVisualSecondaryThickness="{TemplateBinding FocusVisualSecondaryThickness}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" Control.IsTemplateFocusTarget="True" PlaceholderBackground="{ThemeResource GridViewItemPlaceholderBackground}" PointerOverBackground="{ThemeResource GridViewItemBackgroundPointerOver}" PointerOverForeground="{ThemeResource GridViewItemForegroundPointerOver}" PressedBackground="{ThemeResource GridViewItemBackgroundPressed}" RevealBackground="{ThemeResource GridViewItemRevealBackground}" ReorderHintOffset="{ThemeResource GridViewItemReorderHintThemeOffset}" SelectedForeground="{ThemeResource GridViewItemForegroundSelected}" SelectedPointerOverBackground="{ThemeResource GridViewItemBackgroundSelectedPointerOver}" SelectionCheckMarkVisualEnabled="{ThemeResource GridViewItemSelectionCheckMarkVisualEnabled}" SelectedBackground="{ThemeResource GridViewItemBackgroundSelected}" SelectedPressedBackground="{ThemeResource GridViewItemBackgroundSelectedPressed}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="Selected"/>
                                    <VisualState x:Name="PointerOver"/>
                                    <VisualState x:Name="PointerOverSelected"/>
                                    <VisualState x:Name="PointerOverPressed">
                                        <VisualState.Setters>
                                            <Setter Target="Root.(Control.FocusVisualSecondaryThickness)" Value="5"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="Root.(Control.FocusVisualSecondaryThickness)" Value="5"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="PressedSelected">
                                        <VisualState.Setters>
                                            <Setter Target="Root.(Control.FocusVisualSecondaryThickness)" Value="5"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="DisabledStates">
                                    <VisualState x:Name="Enabled"/>
                                    <VisualState x:Name="Disabled"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </ListViewItemPresenter>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <RelativePanel>
        <FontIcon Glyph="" FontFamily="/Assets/Segoe Xbox Symbol v1.07.ttf#Segoe Xbox Symbol" RelativePanel.AlignRightWithPanel="True" FontSize="48" Margin="0,57,98,0"/>
        <StackPanel Orientation="Horizontal" Spacing="8" Margin="100,58">
            <Button MinHeight="64" MinWidth="64" MaxHeight="64" MaxWidth="64" Padding="0">
                <Image Source="/Assets/AppXBranding/SourceIcon.png"/>
            </Button>
        </StackPanel>
        <TextBlock x:Name="ScreenName" Text="Your games &amp; apps" FontSize="48" FontWeight="Light" Margin="100,169,0,0" CharacterSpacing="18"/>
        <GridView x:Name="AppList" Margin="0,49,0,0" Padding="104,0" SelectionMode="None" RelativePanel.Below="ScreenName" ScrollViewer.HorizontalScrollMode="Enabled" ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid ItemHeight="316" ItemWidth="316" MaximumRowsOrColumns="4"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemContainerTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition FromHorizontalOffset="450" FromVerticalOffset="0" IsStaggeringEnabled="True"/>
                    <AddDeleteThemeTransition/>
                </TransitionCollection>
            </GridView.ItemContainerTransitions>
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Image Source="/Assets/AppXBranding/Square150x150Logo.scale-400.png"/>
                        <Grid x:Name="NameHolder" Background="Transparent" VerticalAlignment="Bottom" HorizontalAlignment="Left" MaxWidth="308">
                            <TextBlock Text="Store" FontSize="24" Margin="16,12" FontFamily="Segoe UI" TextWrapping="Wrap"/>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridViewItem Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Avatar" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Captures" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Settings" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Achievements" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Friends" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Activity" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Messages" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Internet Explorer" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="TV" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Party" Background="{ThemeResource SystemAccentColor}"/>
            <GridViewItem Content="Game DVR" Background="{ThemeResource SystemAccentColor}"/>
        </GridView>
        <!-- GridView Margin="0,288,0,146" Padding="100,0" SelectionMode="None" RelativePanel.AlignVerticalCenterWithPanel="True" ScrollViewer.HorizontalScrollMode="Enabled" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid ItemHeight="312" ItemWidth="312" MaximumRowsOrColumns="4"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemContainerTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition FromHorizontalOffset="450" FromVerticalOffset="0" IsStaggeringEnabled="True"/>
                </TransitionCollection>
            </GridView.ItemContainerTransitions>
            <GridViewItem Content="Store" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Avatar" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Captures" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Settings" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Achievements" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Friends" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Activity" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Messages" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Internet Explorer" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="TV" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Party" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
            <GridViewItem Content="Game DVR" Background="{ThemeResource SystemAccentColor}" FocusVisualPrimaryThickness="3" FocusVisualSecondaryThickness="3" FocusVisualSecondaryBrush="Black"/>
        </GridView -->
    </RelativePanel>
</Page>
