<Page
    x:Class="XboxSettings.Pages.RemakeExclusive.UnfaithfulSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XboxSettings.Pages.RemakeExclusive"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromHorizontalOffset="500" FromVerticalOffset="0" IsStaggeringEnabled="True"/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="ScreenName" Text="Unfaithful mode options" VerticalAlignment="Top" FontSize="46" Margin="100,156,0,0" OpticalMarginAlignment="TrimSideBearings" FontWeight="Light" CharacterSpacing="40" FontFamily="Segoe UI"/>
        <ScrollViewer Grid.Row="1" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Hidden" VerticalScrollMode="Disabled">
            <StackPanel Orientation="Horizontal" Padding="100,0,92,0" Spacing="80">
                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="500" FromVerticalOffset="0" IsStaggeringEnabled="True"/>
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>
                <RelativePanel x:Name="AchievementsSettingsBlock" RelativePanel.Below="ScreenName" Margin="0,17,0,0">
                    <TextBlock x:Name="SectionHeader1" Text="SETTINGS FOR &quot;ACHIEVEMENTS&quot;" Style="{ThemeResource GroupHeaderTextBlockStyle}"/>
                    <StackPanel x:Name="AccuracySettingsList" RelativePanel.Below="SectionHeader1">
                        <ComboBox x:Name="UnfaithfulSwitch1" Header="Achivement details screen style" PlaceholderText="Select a style">
                            <ComboBoxItem Content="Achievements (Initial dashboard)"/>
                            <ComboBoxItem Content="Friends (2014)"/>
                        </ComboBox>
                    </StackPanel>
                </RelativePanel>
                <RelativePanel x:Name="MessageSettingsBlock" RelativePanel.Below="ScreenName" Margin="0,17,0,0">
                    <TextBlock x:Name="SectionHeader2" Text="SETTINGS FOR &quot;MESSAGES&quot;" Style="{ThemeResource GroupHeaderTextBlockStyle}"/>
                    <StackPanel x:Name="MessageSettingsList" RelativePanel.Below="SectionHeader2">
                        <CheckBox x:Name="UnfaithfulSwitch2" Content="Threaded messaging"/>
                    </StackPanel>
                </RelativePanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
