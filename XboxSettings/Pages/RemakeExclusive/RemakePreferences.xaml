<Page
    x:Class="XboxSettings.Pages.RemakeExclusive.RemakePreferences"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XboxSettings.Pages.RemakeExclusive"
    xmlns:testing="using:XboxSettings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Background>
        <ImageBrush ImageSource="videoframe_4287551.png"/>
    </Page.Background>

    <Grid>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromHorizontalOffset="500" FromVerticalOffset="0" IsStaggeringEnabled="True"/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="ScreenName" Text="Xbox '13 preferences" VerticalAlignment="Top" FontSize="46" Margin="100,156,0,0" OpticalMarginAlignment="TrimSideBearings" FontWeight="Light" CharacterSpacing="40" FontFamily="Segoe UI"/>
        <ScrollViewer Grid.Row="1" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Hidden" VerticalScrollMode="Disabled">
            <StackPanel Orientation="Horizontal" Padding="100,0,92,0" Spacing="80">
                <StackPanel.ChildrenTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="500" FromVerticalOffset="0" IsStaggeringEnabled="True"/>
                    </TransitionCollection>
                </StackPanel.ChildrenTransitions>
                <RelativePanel x:Name="AccuracyBlock" RelativePanel.Below="ScreenName" RelativePanel.RightOf="SystemSettings" Margin="0,17,0,0">
                    <TextBlock x:Name="SectionHeader1" Text="STYLE OPTIONS" Style="{ThemeResource GroupHeaderTextBlockStyle}"/>
                    <StackPanel x:Name="AccuracySettingsList" RelativePanel.Below="SectionHeader1">
                        <ComboBox x:Name="DashVariantDropdown" Header="Dashboard version" PlaceholderText="Select a dashboard">
                            <x:String>November 2013 (Launch)</x:String>
                            <x:String>February 2014</x:String>
                            <x:String>March 2014</x:String>
                        </ComboBox>
                        <ComboBox x:Name="FontDropdown" Header="System font" SelectedIndex="0" Margin="0,8,0,0" IsEnabled="False">
                            <x:String>Segoe UI</x:String>
                            <x:String>Segoe UI Variable</x:String>
                            <x:String>Bahnschrift</x:String>
                            <x:String>Convection</x:String>
                        </ComboBox>
                        <ComboBoxItem Content="Item"/>
                        <ComboBoxItem Content="Item" IsSelected="True"/>
                    </StackPanel>
                </RelativePanel>
                <RelativePanel x:Name="UnfaithfulBlock" RelativePanel.Below="ScreenName" RelativePanel.RightOf="SystemSettings" Margin="0,17,0,0" MaxWidth="424">
                    <TextBlock x:Name="SectionHeader2" Text="UNFAITHFUL MODE" Style="{ThemeResource GroupHeaderTextBlockStyle}"/>
                    <StackPanel x:Name="UnfaithfulSettingsList" RelativePanel.Below="SectionHeader2">
                        <CheckBox x:Name="UnfaithfulSwitch" Content="Unfaithful mode" Checked="UnfaithfulSwitch_Checked" Unchecked="UnfaithfulSwitch_Unhecked"/>
                        <Button x:Name="UnfaithfulOptionsButton" Content="Unfaithful mode options" IsEnabled="False" Click="UnfaithfulOptionsButton_Click"/>
                        <testing:SplitOpenControl />
                    </StackPanel>
                </RelativePanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
